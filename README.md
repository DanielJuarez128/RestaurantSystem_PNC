## Crear base de datos en Postgres
En **PGAdmin**, ejecutar la siguiente instrucción SQL:
```SQL
CREATE DATABASE restaurant;
```

## Peticiones a la API:

A traves de **Bruno**, se exportaron las peticiones en la carpeta *RestaurantAPI*.