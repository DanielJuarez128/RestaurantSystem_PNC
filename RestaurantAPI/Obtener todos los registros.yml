info:
  name: Obtener todos los registros
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/menu/"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
