info:
  name: Eliminar registro
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE_URL}}/api/menu/{{ID}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​