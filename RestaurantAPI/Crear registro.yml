info:
  name: Crear registro
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/menu/"
  body:
    type: json
    data: |-
      {
        "name":"Caviar rojo",
        "description":"Huevas de boca colorada",
        "price":10000000.00,
        "avalible":false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
